#This is an example of R 

# Creating variables
x <- 11
y = 6

# Variable names 
my_variable <- "Hello, R!" 

# Defining a function
add_numbers <- function(a, b) {
  result <- a + b
  return(result)
}

# Calling the function
sum_result <- add_numbers(3, 4)
print(sum_result)